#!/usr/bin/env bash
set -euo pipefail

# Activate venv if it exists
REPO_ROOT="$(git rev-parse --show-toplevel)"
if [ -f "$REPO_ROOT/.venv/bin/activate" ]; then
    source "$REPO_ROOT/.venv/bin/activate"
fi

echo "Running pre-commit checks (invoke build)..."
invoke build
