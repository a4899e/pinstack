#!/bin/sh
# Pre-commit hook: run full build.

set -e

REPO_ROOT="$(git rev-parse --show-toplevel)"

if ! command -v uv >/dev/null 2>&1; then
  echo "ERROR: uv not found. Install with: curl -LsSf https://astral.sh/uv/install.sh | sh"
  exit 1
fi

cd "$REPO_ROOT"
uv run inv build
