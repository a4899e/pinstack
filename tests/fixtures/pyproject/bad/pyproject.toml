[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "myapp"
version = "1.0.0"
classifiers = [
    "Development Status :: 3 - Alpha",
]
dependencies = [
    "flask>=2.0.0",
    "requests~=2.31.0",
    "certifi",
]
